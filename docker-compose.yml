version: '3'

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl
      - /etc/letsencrypt:/etc/letsencrypt
      - /var/www/certbot:/var/www/certbot    
    depends_on:
      - frontend
    networks:
      - app-network

  mongodb:
    image: mongo:latest
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=Gik8Z1GrTX74
    command: ["--auth", "--bind_ip", "127.0.0.1,mongodb"]
    volumes:
      - mongodb_data:/data/db
      - ./mongodb-init:/docker-entrypoint-initdb.d
    networks:
      - app-network

  backend:
    build: 
      context: ./webshop-server
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - MONGODB_URI=mongodb://admin:Gik8Z1GrTX74@mongodb:27017/webshop?authSource=admin
      - PORT=3001
      - CORS_ORIGIN=https://giannigiovanni.hu
    depends_on:
      - mongodb
    networks:
      - app-network

  frontend:
    build:
      context: ./gianni-app
      dockerfile: Dockerfile
    expose:
      - "80"
    environment:
      - VITE_API_URL=https://giannigiovanni.hu/api
    depends_on:
      - backend
    networks:
      - app-network

volumes:
  mongodb_data:

networks:
  app-network:
    driver: bridge