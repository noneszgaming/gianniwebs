version: '3.8'

services:
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  backend:
    build: 
      context: ./webshop-server
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/webshop
      - PORT=3001
      - CORS_ORIGIN=http://91.214.112.140:80
    depends_on:
      - mongodb

  frontend:
    build:
      context: ./gianni-app
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=http://91.214.112.140:3001
    depends_on:
      - backend

volumes:
  mongodb_data: